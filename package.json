{
  "name": "devutils-extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo \"Nothing to build; load the extension from the project root.\"",
    "zip": "mkdir -p dist && zip -r dist/devutils.zip manifest.json popup.html popup.js styles.css assets"
  }
}
